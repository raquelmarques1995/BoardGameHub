<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- Main Theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light" >

    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.MaterialComponents.Light.NoActionBar" >

    </style>

    <!-- Buttons -->
    <style name="Widget.App.Button" parent="Widget.MaterialComponents.Button">

    </style>

    <!-- EditText -->
    <style name="EditTextStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:background">@drawable/rounded_edit_text</item>
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="android:textSize">16sp</item>
        <item name="android:padding">12dp</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:layout_marginBottom">12dp</item>
        <item name="android:focusable">true</item>  <!-- Garante que o foco seja possível -->
        <item name="android:focusableInTouchMode">true</item> <!-- Habilita o foco quando clicado -->
    </style>

    <!-- AlertDialog -->
    <style name="CustomAlertDialog" parent="Theme.MaterialComponents.DayNight.Dialog.Alert">

    </style>
</resources>